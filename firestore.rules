rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura/escritura a documentos userData para cualquier usuario
    match /userData/{userId} {
      allow read, write: if true;
    }
  }
}
